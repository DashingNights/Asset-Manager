<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Inventory Form</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
	</head>
	<body>
		<div class="container">
			<form id="inventoryForm" method="POST" action="/api/inventory/editmonitor/<%= monitor._id %>?_method=PUT">
				<h2>New Monitor Form</h2>

				<div class="form-group">
					<label for="Item_Holder">Item Holder</label>
					<input type="text" class="form-control" id="Item_Holder" name="Item_Holder" value="<%= monitor ? monitor.Item_Holder : '' %>" />
				</div>
				<div class="card-body">
					<input type="checkbox" name="IsUsed" value="true" <%= monitor && monitor.IsUsed ? 'checked' : '' %>> Is Used<br />
					<input type="text" name="Vendor" value="<%= monitor ? monitor.Vendor : '' %>" /> Vendor<br />
					<input type="text" name="Brand" value="<%= monitor ? monitor.Brand : '' %>" /> Brand<br />
					<input type="number" name="Price" value="<%= monitor ? monitor.Price : 0 %>" /> Price<br />
					<input type="date" name="DatesPurchased" value="<%= monitor ? monitor.Dates.Purchased : '' %>" /> Purchased Date<br />
					<input type="date" name="DatesWarranty_End" value="<%= monitor ? monitor.Dates.Warranty_End : '' %>" /> Warranty End Date<br />
					<input type="text" name="Model" value="<%= monitor ? monitor.Model : '' %>" /> Model<br />
					<input type="text" name="Serial_Number" value="<%= monitor ? monitor.Serial_Number : '' %>" /> Serial Number<br />
					<input type="text" name="Asset_ID" value="<%= monitor ? monitor.Asset_ID : '' %>" /> Asset ID<br />
					<input type="text" name="Monitor_ID" value="<%= monitor ? monitor.Monitor_ID : '' %>" /> Monitor ID<br />
					<input type="number" name="Size" value="<%= monitor ? monitor.Size : 0 %>" /> Size<br />
					<input type="text" name="Resolution" value="<%= monitor ? monitor.Resolution : '' %>" /> Resolution<br />
					<input type="text" name="Remarks" value="<%= monitor ? monitor.Remarks : '' %>" /> Remarks<br />
				</div>
				<br />
				<br />
				<input type="text" class="form-control" id="id" name="id" value="<%= monitor ? monitor._id : '' %>" readonly />

				<button type="submit" class="btn btn-primary">Submit</button>
			</form>
            <button class="btn btn-secondary dedit">Back</button>
                        <script>
                            document.querySelector('.dedit').addEventListener('click', function() {
                                window.location.href = '/triage/data-edit-triage';
                            });
                        </script>
		</div>

		<!-- Bootstrap JS and jQuery -->
		<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
